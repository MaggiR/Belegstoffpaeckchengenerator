Do not install dependencies locally on the host system. The entire project runs via Docker.

After you're done with a batch of implementations, run Docker compose to rebuild (and run) the project and fix any build errors.